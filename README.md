# odin-recipes1
take 3, shall we?

basic html and git skills in action
boilerplate, lang, heading, meta, title, body, paragraph, ol, ul, link, image
git status, git log, git add, git commit, git push origin main